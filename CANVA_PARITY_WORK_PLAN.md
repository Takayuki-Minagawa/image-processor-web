# Pixelweave Studio Canva同等機能 作業計画

- 作成日: 2026-08-01（JST）
- 対象: Canvaと同程度のデザイン制作機能（デザインドキュメント化、素材・テンプレート、テキスト強化、図表、アニメーション、出力拡張、モバイルUI）
- 文書種別: 機能拡張の作業計画（フェーズ分割・受け入れ条件・リスク含む）
- 前提: [初期調査・MVP計画](./IMAGE_EDITOR_WORK_PLAN.md)、[機能拡張計画（実装済み）](./FEATURE_EXPANSION_WORK_PLAN.md)、[実装状況](./docs/IMPLEMENTATION_STATUS.md)

## 0. 方針

### 0.1 現状とCanvaのギャップ

現在のPixelweaveは「1枚の画像を編集するエディタ」であり、Canvaは「複数ページのデザインを素材とテンプレートから組み立てるツール」である。既に同等の機能（レイヤー・ブレンドモード・テキスト装飾・フィルター・ロゴ生成・バッチ書き出し・背景除去・Undo/Redo・自動保存・PWA）は多いが、構造的に欠けているのは次の領域。

| 領域               | Canva                                        | Pixelweave現状                 |
| ------------------ | -------------------------------------------- | ------------------------------ |
| ドキュメントモデル | デザインサイズ起点・複数ページ               | 画像1枚起点・単一キャンバス    |
| レイヤー構造       | グループ・フレーム（クリッピング）           | フラット。グループ・マスクなし |
| 素材               | 図形・アイコン・イラスト・フレーム・グリッド | 矩形・楕円のみ                 |
| テンプレート       | 用途別デザインテンプレート多数               | ロゴ用20種のみ                 |
| フォント           | 多数（日本語含む）・アップロード対応         | Latin可変フォント4種           |
| ブランドキット     | 色・フォント・ロゴの登録と適用               | なし                           |
| 図表               | 表・グラフ                                   | なし                           |
| アニメーション     | ページ遷移・要素アニメ・動画出力             | なし                           |
| 出力               | PNG/JPG/PDF/SVG/GIF/MP4                      | PNG/JPEG/WebP/SVG              |
| マジックリサイズ   | サイズ変更時に自動再レイアウト               | なし                           |
| モバイルUI         | 完全対応                                     | 対象外（既知の制限）           |
| クラウド・共同編集 | あり                                         | 意図的に対象外                 |

### 0.2 維持する原則

MVP以来の原則を本計画でもすべて維持する。逸脱が必要な場合は個別ADRで判断する。

- ローカルファースト。画像・素材・フォントを含め、既定でネットワーク送信しない
- `.pwx.json`プロジェクト形式を正本とし、Fabric.js JSONは分離されたrenderer payloadに留める（ADR-002/004）
- 追加機能はすべてUndo/Redo・自動保存・unit/E2Eテストの対象に含める
- 重い処理はWeb Workerへ分離し、メインスレッドを塞がない
- エントリバンドル予算（`scripts/check-bundle-budget.mjs`）を守る。大きな資産（フォント・素材・テンプレート）はすべて遅延読み込みチャンクにする
- 厳格CSP（`script-src 'self' 'wasm-unsafe-eval'`）を維持する
- クラウド同期・共同編集・生成AIは本計画でも**対象外**（C9参照）。着手する場合は別ADR＋別計画とする

### 0.3 実装順の考え方

「後続フェーズの土台になるものを先に」並べる。特に次の2点は全フェーズの前提であり、C0として最初に完了させる。

1. `App.tsx`（約4,700行・useState 50個超）の分割と状態管理の整理。以降のフェーズはすべてUIを増やすため、放置するとマージ衝突とバグの温床になる
2. i18n体制の拡張。現在Studioパネル群は日本語ハードコードであり、新規UIを同じ方式で増やすと後から回収不能になる

各フェーズは独立してPR化・リリース可能な単位とする。

## 1. フェーズ一覧

| フェーズ | 内容                                                                     | 主な成果物                   | 依存       | 目安規模 |
| -------- | ------------------------------------------------------------------------ | ---------------------------- | ---------- | -------- |
| C0       | 基盤リファクタリング（App分割・状態管理・i18n拡張）                      | 以降の開発速度と安全性       | なし       | 中〜大   |
| C1       | レイヤーグループ・クリッピングマスク・レイヤーマスク                     | Canva的レイヤー構造          | C0         | 大       |
| C2       | デザインドキュメント化（サイズプリセット・複数ページ・マジックリサイズ） | デザイン起点のワークフロー   | C0, C1     | 大       |
| C3       | 素材ライブラリ（図形・アイコン・フレーム・グリッド・ユーザー素材）       | 「素材を置いて作る」体験     | C1         | 中〜大   |
| C4       | テキスト・フォント強化（日本語フォント・フォント追加・テキストスタイル） | 日本語デザイン対応           | C0         | 中       |
| C5       | テンプレート・ブランドキット                                             | ワンクリックで整ったデザイン | C2, C3, C4 | 大       |
| C6       | 表・グラフ                                                               | 資料系デザイン対応           | C3         | 中       |
| C7       | 出力拡張（PDF・印刷向け設定・GIF）                                       | 配布・印刷ワークフロー       | C2         | 中       |
| C8       | アニメーション・動画書き出し                                             | 動くデザイン                 | C2, C7     | 大       |
| C9       | モバイル・レスポンシブUI                                                 | タブレット/スマホ対応        | C0〜C5     | 大       |
| —        | （対象外継続）クラウド同期・共有リンク・共同編集・生成AI                 | —                            | 別ADR      | —        |

推奨リリース単位: **C0→C1→C2**で「デザインツールとしての骨格」、**C3→C4→C5**で「Canva的な体験」、**C6→C7→C8→C9**は独立性が高く順序入れ替え可。

## 2. フェーズ詳細

### C0: 基盤リファクタリング

以降の全フェーズの前提。機能追加を含まないため、E2Eゴールデンスクリーンショットを含む既存テスト全通過を「振る舞い不変」の証明として使う。

作業項目:

1. **App.tsxの分割** — `App.tsx`（4,681行）を、シェル / ツールレール / キャンバスエリア / インスペクター / ダイアログ群のコンポーネントへ分割する。エディタ状態は`useReducer`＋Contextまたは軽量ストア（zustand等、追加する場合はバンドル予算内で判断しADR化）へ集約し、`FabricEditorEngine`への参照とコールバック配線を1箇所にまとめる
2. **fabricEngine.tsの分割準備** — 4,218行の`fabricEngine.ts`を、責務単位（document/objects/text/selection/guides/io）のモジュールへ内部分割する。公開APIは`FabricEditorEngine`クラスのまま維持し、外部から見た境界を変えない
3. **i18n拡張** — `EditorUiCopy`をネームスペース分割し、Studioパネル・フィルター名・ブレンドモード名・履歴ラベル・エラーメッセージのハードコード日本語をすべてi18n経由へ移行する。英語カタログの遅延読み込み方式は維持する
4. **履歴のスケール確認** — ADR-003の移行トリガー（4K×20レイヤー×100操作）をC1/C2実装前に計測し、スナップショット履歴を維持するか差分型へ移行するかを決定してADRを更新する。C2の複数ページ化はスナップショットサイズを直撃するため、この判断はC2着手前に必須

受け入れ条件:

- 既存unitテスト・E2E（ゴールデンスクリーンショット含む）が全通過し、バンドル予算を超えない
- `App.tsx`が1,000行未満になり、新規パネル追加が独立ファイルの追加で完結する
- `npm run lint`でハードコードUI文字列を検出する仕組み（ESLintルールまたはスクリプト）が導入されている

リスク: 振る舞い不変のリファクタリングとして最大級。「状態管理の集約」「コンポーネント分割」「i18n移行」を別PRに分け、各PRでE2E全通過を確認する。

### C1: レイヤーグループ・クリッピングマスク・レイヤーマスク

Canvaの「フレーム」「グループ化」に相当する構造。C2のページ、C3の素材（フレーム素材）、C5のテンプレートすべての土台になる。

作業項目:

1. **レイヤーグループ** — 複数レイヤーのグループ化/解除（⌘G/⇧⌘G）、グループ単位の移動・変形・不透明度・表示/ロック、レイヤーパネルのツリー表示（折りたたみ・グループ内並べ替え・グループ間ドラッグ移動）。Fabric.jsの`Group`を使うが、`editorId`ツリーを正本としFabric構造への依存を最小化する
2. **クリッピングマスク** — 図形（矩形・楕円・後続のC3フレーム素材）で画像を切り抜く「フレーム」機能。画像のフレームへのドロップイン、フレーム内での画像の位置・スケール調整モード（ダブルクリックで進入）、切り抜き解除
3. **レイヤーマスク** — 既存の8bit `SelectionMask`基盤（`src/selection/`）を流用し、レイヤーごとのグレースケールマスクを実装する。選択範囲からマスク作成、マスクのブラシ編集（白/黒/グレー）、マスクの有効/無効・削除・適用（ラスタライズ）
4. **既存機能との整合** — 整列・分布・スナップ・自動化コマンドのsemantic target・SVG書き出しがグループとマスクを正しく扱うことを保証する

`.pwx.json`への影響: schema version 3へ更新。レイヤーツリー（グループの入れ子）、クリッピング参照、レイヤーマスクpayload（既存のraw/RLE losslessコーデックを流用）を`editorState`側に追加し、v2読み込み互換を維持する。

受け入れ条件:

- グループの入れ子（3階層以上）を作成・変形・保存→再読込しても構造と見た目が保持される
- 画像をフレームへドロップ→内部で位置調整→PNG/SVG書き出しで切り抜きが正しく反映される
- レイヤーマスクで部分的に透過した合成が書き出しに反映され、マスク編集がUndo/Redo可能
- グループ・マスクを含むプロジェクトで既存の調整レイヤー・選択フィルターが破綻しない（unit＋E2E）

リスク: Fabric.js 7の`Group`は座標系・イベント伝播に癖がある。エンジン内で正規化する既存方針（中心原点→左上原点の正規化と同様）を踏襲し、Fabric依存をadapter内に閉じ込める。

### C2: デザインドキュメント化

「画像を開く」から「デザインを作る」への転換。Canvaの中核概念。

作業項目:

1. **デザインサイズプリセット** — 新規作成ダイアログを刷新し、SNS投稿（Instagram正方形/ストーリー、X、YouTubeサムネイル等）、プレゼンテーション16:9、A4縦横、名刺、バナー各種などのプリセット＋カスタムサイズ＋単位（px/mm、mmはDPI指定付き）を提供する。既存の`batch/iconPresets.ts`のプリセット機構を汎用化して流用する
2. **複数ページ** — 1プロジェクト内に複数ページ（キャンバス）を持てるようにする。ページの追加・複製・削除・並べ替え、ページ間のオブジェクトコピー、ページサムネイル一覧UI、ページ単位・全ページ一括の書き出し。Fabricキャンバスは常に1枚とし、非アクティブページはシリアライズ済みpayloadとサムネイルで保持する（メモリ対策）
3. **マジックリサイズ** — デザインサイズ変更時に、アンカー（9方向）＋スケール方式の再レイアウトを提供する。全要素の相対位置・相対サイズを基準に再配置し、テキストはフォントサイズを比例スケールする。結果は通常のUndo 1回で戻る
4. **背景概念** — ページごとの背景（単色・グラデーション・画像）を通常レイヤーと区別して扱い、素材ドロップ時の背景差し替えを可能にする

`.pwx.json`への影響: schema version 3（C1と同時に設計する）。ドキュメント→ページ配列→レイヤーツリーの3層構造へ拡張し、v2プロジェクトは「1ページのドキュメント」として読み込む。自動保存（OPFS）はページ単位の差分書き込みへ変更し、大規模プロジェクトでの自動保存時間を抑える。

受け入れ条件:

- プリセットから作成→ページ追加→ページごとに編集→保存/再読込で全ページが復元される
- 10ページ・各ページ4K相当のプロジェクトで、ページ切り替えが1秒以内、自動保存がUI操作をブロックしない
- マジックリサイズ（例: Instagram正方形→ストーリー縦長）で全要素がキャンバス内に収まり、Undo 1回で完全に戻る
- 全ページ一括PNG書き出し（ZIPまたはフォルダ）が既存バッチ基盤経由で動作する

リスク: 履歴（スナップショット型）へのページ導入はサイズ増が大きい。C0-4の履歴判断に従い、最低でも「アクティブページのみスナップショット＋ページ構造の軽量記録」とする。

### C3: 素材ライブラリ

作業項目:

1. **基本図形の拡充** — 多角形、星、矢印、吹き出し、線（直線・エルボー・矢印付き、太さ・破線）、角丸矩形（半径調整ハンドル付き）をベクターオブジェクトとして追加する
2. **アイコン・イラスト素材** — OFL/CC0/MITなど再配布可能なライセンスのSVGアイコンセット（例: Lucide系）を素材化する。全件をバンドルせず、カテゴリ単位の遅延読み込みチャンク＋検索インデックス（名称・タグの日英対応）で提供する。挿入時は既存の`svgSafety.ts`サニタイザを必ず通す
3. **フレーム素材** — C1のクリッピングマスクを使う定型フレーム（円・角丸・アーチ・デバイスモックアップ等）
4. **グリッド・レイアウト素材** — 写真グリッド（2分割・3分割・コラージュ）。セルへのドロップイン、セル境界のドラッグ調整
5. **ユーザー素材** — ユーザーが画像・SVGを「マイ素材」として登録し、プロジェクト横断で再利用できるようにする。保存先はOPFS（既存の背景モデルキャッシュと同じ機構）、容量上限と一覧・削除UIを設ける。ローカルファースト原則により同期はしない
6. **素材パネルUI** — 左レールに素材パネル（検索・カテゴリ・最近使用・マイ素材）を追加し、ドラッグ&ドロップとクリック挿入の両方に対応する

受け入れ条件:

- 素材パネルからの挿入がすべてUndo可能で、`.pwx.json`保存→再読込で保持される
- 素材チャンクがエントリバンドル予算に含まれない（bundle budgetスクリプトで検証）
- script入りSVGをマイ素材へ登録しても実行されない（既存sanitizerのunitテストを拡張）
- 写真グリッドへ画像2枚をドロップ→境界調整→書き出しが正しく動く

リスク: 素材の量とバンドルサイズ。初期セットは厳選（数百個程度）し、ライセンス表記を`docs/`に集約する。素材の追加パックはC5のテンプレートと同じ配布形式（検証付きJSON＋SVG）で拡張可能にしておく。

### C4: テキスト・フォント強化

日本語デザインツールとして成立するための必須フェーズ。

作業項目:

1. **日本語フォント同梱** — Noto Sans JP / Noto Serif JP等のOFLフォントをサブセット化（可変フォント＋unicode-range分割）して自己ホストする。エントリバンドルには含めず、フォント選択時に遅延読み込みする。ロゴ用Latinフォント4種と統合したフォントレジストリ（`src/logo/fonts.ts`を汎用化）を作る
2. **ユーザーフォント読み込み** — ローカルのフォントファイル（woff2/ttf/otf）を`FontFace` APIで読み込み、OPFSへ保存してプロジェクト横断で使えるようにする。`.pwx.json`にはフォント名と代替フォントのみ記録し、フォントバイトは埋め込まない（ライセンス配慮を明記）。Local Font Access API対応環境ではインストール済みフォント一覧からの選択も提供する
3. **テキストスタイル** — 見出し/小見出し/本文のスタイルプリセット、縦書き（日本語）、箇条書き・番号リスト、テキストボックス内の折り返しと自動リサイズモード（Canvaの3モード相当: 自動拡張・折り返し・固定）
4. **テキストエフェクトプリセット** — 既存の縁取り・シャドウ・グラデーション・円弧を組み合わせた定型エフェクト（ネオン、スプライス、背景付き、エコー等）をワンクリック適用のプリセットとして提供する

受け入れ条件:

- 日本語フォントの初回選択時のみネットワークではなく**自己ホストチャンク**から読み込まれ、オフライン（PWA）でも2回目以降使用できる
- ユーザーフォントを使ったプロジェクトを、そのフォントがない環境で開いた場合に警告＋代替フォント表示になり、データは失われない
- 縦書きテキストがPNG/SVG書き出しで正しく描画される
- フォント読み込みがエントリバンドル予算に影響しない

リスク: 日本語フォントはサブセット後も大きい。unicode-range分割の粒度とキャッシュ戦略（Service Workerのランタイムキャッシュ対象に追加）を先に設計する。縦書きはFabric.jsが未対応のため、エンジン内での自前レイアウト（1文字ずつの配置）になる可能性が高く、対応範囲（回転すべき約物等)を最初に限定する。

### C5: テンプレート・ブランドキット

Canva体験の中核。C2（ページ・サイズ）、C3（素材）、C4（フォント）が揃ってから着手する。

作業項目:

1. **テンプレートスキーマの汎用化** — ロゴ用テンプレート機構（`src/logo/templateSchema.ts`、検証付きJSON）を、任意サイズ・複数ページ・素材参照・画像プレースホルダを持つデザインテンプレートへ拡張する
2. **組み込みテンプレート** — 主要カテゴリ（SNS投稿、サムネイル、バナー、名刺、A4チラシ、プレゼン）で各カテゴリ数点、計30〜50点を制作する。遅延読み込みチャンク＋サムネイル一覧で提供する
3. **テンプレートブラウザ** — 新規作成フローに統合（サイズ選択→テンプレート選択→編集開始）。適用後はすべて通常の編集可能レイヤーに展開される（ロゴ生成と同じ方針）
4. **ユーザーテンプレート** — 現在のデザインをテンプレートとして保存（`.pwxtemplate.json`）・書き出し・読み込み。マクロと同様にschema version・検証・未知要素の安全スキップを実装する
5. **ブランドキット** — ブランドカラーパレット（複数）、ブランドフォント（見出し/本文の割り当て）、ブランドロゴ（マイ素材参照）を登録し、テンプレート適用時・カラーピッカー・フォント選択に常時表示する。保存先はOPFS

受け入れ条件:

- テンプレートから開始→画像・テキスト差し替え→書き出しの一連フローが3分以内に完了できる（E2Eで手順を固定）
- テンプレート適用がUndo 1回で戻る
- 不正・未知要素を含む`.pwxtemplate.json`がクラッシュせず警告になる（unitテスト）
- ブランドキット登録後、カラーピッカーとフォント選択にブランド項目が表示される

リスク: テンプレート制作は実装と別の制作コストがかかる。スキーマとブラウザUIを先行実装し、テンプレート本数は継続的に追加する運用とする。

### C6: 表・グラフ

作業項目:

1. **表** — 行×列の表オブジェクト（セル結合なしの簡易版から開始）。セル単位のテキスト編集・背景色・罫線スタイル、行列の追加・削除・サイズ調整
2. **グラフ** — 棒・横棒・折れ線・円・ドーナツの5種。データはインラインのミニ表エディタで編集し、系列色はブランドキット/パレットと連動する。描画は依存ライブラリを追加せず、renderer非依存の純粋ロジック（データ→ベクター形状群）として実装し、書き出しはSVG/PNG両対応にする
3. **データ入出力** — CSV貼り付けでのデータ取り込み

受け入れ条件:

- 表・グラフの編集がUndo/Redo・保存/再読込・SVG書き出しに対応する
- CSV貼り付け→グラフ化→色変更の一連がE2Eで検証される
- グラフロジックが`src/`配下のrenderer非依存モジュールとしてunitテストされる

リスク: スコープ肥大。セル結合・複合グラフ・散布図などは初期対象外と明記する。

### C7: 出力拡張（PDF・印刷・GIF）

作業項目:

1. **PDF書き出し** — 全ページ/選択ページのPDF出力。まずはページごとのラスター（高DPI PNG）埋め込みPDFを自前生成（依存追加はバンドル予算とCSP内で判断、生成はWorkerで実行）し、ベクター保持PDFは将来課題として分離する
2. **印刷向け設定** — mm単位ドキュメント（C2）に対する塗り足し（bleed）・トンボの付与、DPI指定（150/300/350）、書き出し寸法の事前表示
3. **GIF書き出し（静止画スライドショー）** — 複数ページをフレームとした簡易GIF（ページ表示時間指定）。エンコードはWorkerで実施。C8のアニメーションGIFの土台になる
4. **書き出しダイアログ刷新** — 形式・範囲（ページ選択）・品質・スケール/DPI・透過を1画面に統合し、推定ファイルサイズを表示する

受け入れ条件:

- A4・300DPI・塗り足し3mmのPDFが正しい寸法で出力される（寸法検証をunitテスト化）
- 10ページのPDF書き出し中もUIが操作可能で、キャンセルできる
- 生成物がmacOSプレビュー/Acrobat/Chromeで開ける（E2Eはバイト構造検証＋手動確認手順を記録）

### C8: アニメーション・動画書き出し

最も大きい新領域。C2のページとC7のエンコード基盤の上に載せる。

作業項目:

1. **アニメーションモデル** — 要素単位の入場/退場/強調アニメーション（フェード・スライド・ズーム・ワイプ等のプリセット）、開始タイミング（ページ表示後・前の要素の後）、時間・イージング。タイムライン全編集ではなくCanva同様の「プリセット適用」型から始め、モデルはrenderer非依存の純粋ロジック（時刻→各要素の変換状態）として実装する
2. **ページ遷移** — ページ間のトランジション（なし・フェード・スライド）と表示時間
3. **プレビュー再生** — エディタ内での再生/停止、再生ヘッド。オーバーレイ描画（マーチングアンツと同様、書き出し・プロジェクトへ混入しない）
4. **動画書き出し** — WebCodecs（対応環境）でWebM/MP4を、fallbackでアニメGIFをWorkerで生成する。フレームレンダリングは既存のオフスクリーンdocument canvas経路を流用する
5. **プレゼンテーションモード** — 全画面でのページ送り再生（キーボード操作）

`.pwx.json`への影響: 要素・ページへのアニメーション定義追加（schema version 4想定）。アニメーションなしのプロジェクトは完全に従来通り読み書きできること。

受け入れ条件:

- アニメーション付き3ページのデザインが、プレビュー再生と書き出し動画で同一のタイミング・見た目になる（フレーム抽出比較のE2E）
- WebCodecs非対応環境でGIF fallbackが機能する
- 書き出し中の進捗・キャンセルが機能し、メインスレッドが塞がらない
- アニメーション未使用時の動作・ファイル形式が完全に後方互換

リスク: WebCodecsのブラウザ差・コーデック差。対応マトリクスを最初に調査し、MP4(H.264)が不可の環境ではWebM/GIFのみ提供と明記する。音声は対象外とする。

### C9: モバイル・レスポンシブUI

これまで明示的に対象外だった領域。C0の分割済みUIが前提。

作業項目:

1. **レスポンシブレイアウト** — インスペクター・素材パネルのボトムシート化、ツールレールの下部配置、ダイアログの全画面化。ブレークポイント設計とCSS変数の整理
2. **タッチ操作** — ピンチズーム・2本指パン、タッチ用の大きい変形ハンドル、長押しコンテキストメニュー。既存のポインター系実装（lasso等）のタッチ対応監査
3. **モバイルE2E** — PlaywrightのデバイスエミュレーションでモバイルviewportのE2E・axe監査を追加する

受け入れ条件:

- iPhone/iPad相当のviewportで、テンプレートから作成→編集→書き出しの主要フローが完結する
- タッチ操作のE2Eとaxe監査がCIで通過する
- デスクトップのゴールデンスクリーンショットに回帰がない

### 対象外の継続確認（クラウド・共同編集・生成AI）

Canvaとの最大の差分だが、ローカルファースト原則（サーバー不要・データ送信なし）と根本的に衝突するため、本計画では引き続き対象外とする。将来検討する場合は次を条件とする。

- 個別ADRで「何のデータが・どこへ・どの同意で」送られるかを定義する
- オプトイン方式とし、未設定時は現状と完全に同一の挙動を保つ
- 共有の第一歩としては、サーバー不要の「`.pwx.json`＋使用素材の同梱パッケージ書き出し」（受け渡し用単一ファイル）をC5の追加項目として検討可能

## 3. 横断的事項

### 3.1 スキーマとマイグレーション

- C1＋C2で**schema version 3**（レイヤーツリー・ページ・マスク）、C8で**version 4**（アニメーション）を予定。C1とC2は同時に設計し、schema改版を1回に抑える
- 各改版で「旧version読み込み互換・新規保存は新version・未知versionは拒否」の既存方針（`src/editor/project.ts`）を維持し、`snapshotValidation.ts`とマイグレーションのunitテストを必須とする

### 3.2 性能と予算

- エントリバンドル予算は現行値を維持する。素材・フォント・テンプレート・エンコーダはすべて遅延チャンクとし、`check-bundle-budget.mjs`にチャンク分類の検証を追加する
- C2着手前にADR-003の履歴計測を実施（C0-4）。複数ページ×大画像でスナップショット履歴が破綻する場合、履歴改修をC2の先行タスクに昇格させる
- レンダラー（Fabric.js）はC8まで維持を前提とする。C8のプレビュー再生でフレーム落ちが予算（30fps）を割る場合のみ、ADR-002のエスケープハッチ（PixiJS/WebGPU）を発動検討する

### 3.3 テスト・CI

- 既存方針を踏襲: 純粋ロジックはunitテスト、adapter境界はcomponentテスト、ユーザーフローはPlaywright E2E＋axe監査＋ゴールデンスクリーンショット
- フェーズごとにE2Eを最低1本追加し、`feature-expansion.spec.ts`と同様の性能アサーション（ページ切替時間・書き出し中の応答性）を含める
- 素材・テンプレート・フォントのライセンス一覧を`docs/LICENSES.md`（新設）に集約し、CIで参照切れを検証する

### 3.4 ドキュメント

- 各フェーズ完了時に`docs/IMPLEMENTATION_STATUS.md`を更新する
- 新規ADR想定: レイヤーツリーモデル（C1）、複数ページと履歴（C2）、素材・テンプレート配布形式（C3/C5）、フォント取り扱いとライセンス（C4）、アニメーションモデルと動画エンコード（C8）

## 4. 全体リスク

| リスク                             | 影響                               | 対策                                                                               |
| ---------------------------------- | ---------------------------------- | ---------------------------------------------------------------------------------- |
| C0の遅延・不完全実施               | 全フェーズの開発効率悪化・衝突多発 | C0を最優先で完了。機能PRとリファクタPRを混ぜない                                   |
| スナップショット履歴の限界         | C2以降でUndoが遅く・重くなる       | C0-4で計測し、必要なら履歴改修を先行                                               |
| Fabric.js Groupの制約              | C1の工数超過                       | adapter内正規化。スパイクを最初の1週間で実施し、想定外ならC1計画を修正             |
| 日本語フォント・素材のサイズ       | PWAキャッシュ肥大・初回体験悪化    | サブセット化・遅延チャンク・容量表示付きの明示的ダウンロード（背景モデルと同方式） |
| テンプレート制作コスト             | C5の価値が実装後も出ない           | スキーマ先行・本数は継続追加。ユーザーテンプレート機能で補完                       |
| WebCodecs互換性                    | C8の書き出しが環境依存             | 事前調査で対応マトリクス確定。GIF fallbackを必須実装                               |
| スコープ肥大（表・グラフ・アニメ） | 各フェーズの長期化                 | 各フェーズに「対象外」を明記済み。追加要望は次期計画へ回す                         |
