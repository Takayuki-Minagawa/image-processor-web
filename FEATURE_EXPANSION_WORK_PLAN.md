# Pixelweave Studio 機能拡張 作業計画

- 作成日: 2026-07-31（JST）
- 対象: ロゴ自動生成、GIMP的自動化ツール、エフェクト拡充、および周辺機能
- 文書種別: 機能拡張の作業計画（フェーズ分割・受け入れ条件・リスク含む）
- 前提: [初期調査・MVP計画](./IMAGE_EDITOR_WORK_PLAN.md)、[実装状況](./docs/IMPLEMENTATION_STATUS.md)

## 0. 方針

MVPで確立した次の原則をすべての機能拡張で維持する。

- ローカルファースト。画像・モデル・スクリプトを含め、既定でネットワーク送信しない
- `.pwx.json`プロジェクト形式を正本とし、描画ライブラリ（Fabric.js）のJSONを正本にしない
- 追加機能はすべてUndo/Redo・自動保存・E2Eテストの対象に含める
- 重い処理はWeb Worker + OffscreenCanvasへ分離し、メインスレッドを塞がない

実装順は「後続フェーズの土台になるものを先に」並べている。各フェーズは独立してPR化・リリース可能な単位とする。

## 1. フェーズ一覧

| フェーズ | 内容                                                 | 主な成果物                            | 依存           | 目安規模 |
| -------- | ---------------------------------------------------- | ------------------------------------- | -------------- | -------- |
| P1       | SVG入出力・テキスト装飾強化・整列/ガイド/スナップ    | ロゴ制作の土台                        | なし           | 小〜中   |
| P2       | 操作コマンド層・マクロ記録/再生・レシピ保存          | 自動化の基盤（Script-Fu相当の第一歩） | なし           | 中〜大   |
| P3       | Workerパイプライン・バッチ処理・アイコン一括書き出し | 複数ファイルの一括変換                | P2             | 中       |
| P4       | フィルタ拡充・調整レイヤー（非破壊エフェクト）       | エフェクトメニューの大幅増強          | なし（P2推奨） | 中〜大   |
| P5       | 配色支援・ロゴテンプレートジェネレーター             | ロゴ候補の一括生成                    | P1             | 中       |
| P6       | 選択ツール強化（自動選択・なげなわ・ぼかし）         | エフェクトの部分適用                  | P4             | 中〜大   |
| P7       | 背景除去（ローカルAI）・スクリプトコンソール         | 差別化機能                            | P2, P3         | 大       |

## 2. フェーズ詳細

### P1: ロゴ制作の土台（SVG・テキスト装飾・整列）

ロゴ生成（P5）の前提となる編集機能を先に揃える。Fabric.jsの標準機能で大半を実装できるため、最初のフェーズとして着手コストが低い。

作業項目:

1. **SVG読み込み** — `loadSVGFromString`によるドラッグ&ドロップ / ファイル選択 / 貼り付け対応。読み込んだSVGはグループ化されたベクターレイヤーとして配置する。`imageSafety.ts`にSVG用のサニタイズ（script要素・外部参照の除去、サイズ上限）を追加する
2. **SVG書き出し** — `toSVG`によるキャンバス全体・選択オブジェクトの書き出し。ラスターレイヤーは埋め込み画像として出力されることを明記する
3. **テキスト装飾** — 縁取り（stroke幅・色、外側描画）、グラデーション塗り、シャドウ、字間・行間、円弧に沿ったパステキスト
4. **整列・分布** — 選択オブジェクトの左/中/右・上/中/下揃え、等間隔分布、キャンバス中央配置
5. **ガイドとスナップ** — ルーラーからのガイド作成、オブジェクト端・中心・ガイド・キャンバス中心へのスナップ（許容距離設定付き）

受け入れ条件:

- 代表的なSVG（単純図形、パス、テキスト含む）を読み込み、編集して`.pwx.json`保存 → 再読込しても内容が保持される
- script入りSVGを読み込んでもscriptが実行されない（unitテストで担保）
- 縁取り＋グラデーション＋円弧テキストを組み合わせたロゴがPNG/SVG両方で書き出せる
- 整列・スナップ操作がUndo/Redo可能

`.pwx.json`への影響: ベクターオブジェクト（SVG由来グループ、パステキスト）の表現を追加するため、schema version 2へ更新し、v1の読み込み互換を維持する。

### P2: 自動化基盤（操作コマンド層・マクロ・レシピ）

現在の履歴（`history.ts`）はスナップショット型で「何を行ったか」を記録していないため、マクロ記録の前提として操作コマンド層を新設する。これがGIMPのScript-Fu相当の自動化に向けた最重要フェーズ。

作業項目:

1. **操作コマンド層** — `App.tsx`と`fabricEngine.ts`に分散している編集操作を、直列化可能なコマンド（例: `{ type: 'resizeCanvas', width, height }`、`{ type: 'applyFilter', filter: 'sharpen', params }`）として定義し、単一のディスパッチャ経由で実行する構造へ段階的にリファクタリングする。既存のスナップショット履歴は維持し、コマンド層は記録用として並走させる（履歴機構の置き換えは行わない）
2. **マクロ記録・再生** — 記録開始/停止UI、記録中の操作をコマンド列として蓄積、名前を付けて保存、現在のドキュメントへ再生。再生全体を1つのUndo単位にする
3. **パラメータ化** — 「対象サイズ」「テキスト内容」など、再生時に確認ダイアログで上書きできる引数を持つコマンドを定義する
4. **レシピの保存・共有** — マクロを`.pwxmacro.json`として書き出し・読み込み。schema versionとアプリバージョンを埋め込み、未知コマンドは安全にスキップして警告表示する

受け入れ条件:

- 「リサイズ → フィルタ → テキスト透かし追加」を記録し、別画像に再生して同じ結果が得られる
- 再生結果がUndo 1回で完全に戻る
- 書き出したマクロJSONを読み込み、再実行できる
- 不正・未知のコマンドを含むマクロJSONがクラッシュせず警告になる（unitテスト）

リスク: `App.tsx`（約3,000行）の操作ロジック抽出は影響範囲が広い。フェーズ内をさらに「コマンド層のみ」「記録UI」の2 PRに分割し、各段階で既存E2Eの全通過を確認する。

### P3: バッチ処理とWorkerパイプライン

作業項目:

1. **Worker実行基盤** — OffscreenCanvasを使い、デコード → コマンド列適用 → エンコードをWorkerで実行するパイプライン。`ImageBitmap`/`ArrayBuffer`はTransferableとして移動しコピーしない
2. **バッチUI** — 複数ファイル選択（対応環境ではFile System Access APIでフォルダ選択）、適用するマクロまたは定型処理（リサイズ・形式変換・透かし）の選択、進捗表示・キャンセル・失敗ファイルの一覧
3. **出力** — 対応環境ではフォルダへ直接保存、fallbackはZIP一括ダウンロード（ZIP生成もWorkerで実施）
4. **アイコン一括書き出し** — 現在のドキュメントから favicon 16/32/48、PWA 192/512、Apple Touch 180、OGP 1200×630 等のプリセット一括生成。プリセットはユーザー定義可能にする

受け入れ条件:

- 50ファイルのバッチ変換中もUIが操作可能（メインスレッドのロングタスクをE2Eで計測）
- キャンセルで進行中の処理が停止し、完了分のみ出力される
- File System Access API非対応ブラウザでZIPダウンロードに自動fallbackする
- アイコン書き出しで全サイズのPNGが正しい寸法で生成される（unitテスト）

制約の明示: ブラシストローク等のポインター依存操作はWorker再生の対象外とし、バッチで使えるコマンドをUI上で明示する。

### P4: エフェクト拡充と調整レイヤー

作業項目:

1. **組み込みフィルタの露出** — Fabric.js内蔵のWebGLフィルタ（シャープ、エンボス、ノイズ、ピクセレート、セピア、色反転 等）をフィルタメニューへ追加。各フィルタにプレビュー付きパラメータUIを用意する
2. **カスタムフィルタ** — レベル補正、トーンカーブ（スプライン編集UI）、ホワイトバランス、ビネット、グラデーションマップ/デュオトーン、ハーフトーン、グリッチ風。WebGLシェーダ実装を基本とし、WebGL不可環境ではCPU実装へfallbackする
3. **調整レイヤー** — フィルタを画像へ焼き込まず、レイヤースタック上の「調整レイヤー」として保持する非破壊構造。下位レイヤー合成結果への適用、表示切替、パラメータ再編集、通常レイヤーへの焼き込み（ラスタライズ）
4. **フィルタプリセット** — パラメータ組み合わせの保存・適用。P2のレシピ形式に相乗りする

受け入れ条件:

- 全フィルタがUndo/Redo・プロジェクト保存/再読込・PNG書き出しで一貫した結果になる
- 調整レイヤーのパラメータを保存 → 再読込後に再編集できる
- WebGL無効環境（Playwrightで強制）でも主要フィルタが動作する
- 4096×4096画像への単一フィルタ適用が実用時間内（目安2秒以内）で完了する

`.pwx.json`への影響: 調整レイヤー種別の追加（schema拡張、旧版読み込み互換維持）。

### P5: 配色支援とロゴテンプレートジェネレーター

作業項目:

1. **配色抽出** — 読み込み画像から主要色パレットを抽出（ピクセル集計＋量子化。中央値分割法程度で十分）。抽出色はスウォッチとして塗り・縁取りに適用できる
2. **配色ルール提案** — 基準色から補色・類似色・トライアド・モノクロマティックのパレットを生成するUI
3. **テンプレート定義** — 「テキストスロット＋図形＋レイアウト＋配色スロット」をJSONで定義するテンプレート形式。初期セットとして20種類程度を同梱する
4. **ジェネレーターUI** — 名称・イニシャル・任意のタグラインを入力 → テンプレート×配色の候補をグリッドで一括プレビュー → 選択した候補を編集可能なレイヤー群としてキャンバスに展開する
5. **バリエーション再生成** — 配色・フォント・レイアウトを固定/シャッフルして候補を再抽選できる

受け入れ条件:

- 名称入力から10件以上の候補が2秒以内にプレビュー表示される
- 展開後のロゴが通常のオブジェクトとして編集・整列・書き出しできる（P1機能との結合E2E）
- テンプレートJSONの不正データがクラッシュせず該当テンプレートのみ除外される
- 生成したロゴをP3のアイコン一括書き出しへ直接渡せる

同梱フォントはOFL等の再配布可能ライセンスのみを使用し、ライセンス表記をアプリ内に含める。

### P6: 選択ツール強化

エフェクト（P4）の部分適用を可能にし、既存機能の価値を引き上げる。ピクセル選択はMVPで意図的に見送った領域であり、本計画で最も描画エンジンに踏み込むフェーズ。

作業項目:

1. **選択マスク基盤** — ドキュメントに1枚の選択マスク（8bitアルファ）を導入し、以後の塗り・フィルタ・削除が選択範囲を尊重する構造にする
2. **多角形・なげなわ選択** — パス描画による選択、追加/除外（Shift/Alt）の合成モード
3. **自動選択（マジックワンド）** — 許容値付きの色域flood fill。大画像向けにWorkerで実行する
4. **選択範囲の調整** — ぼかし（フェザー）、拡張/縮小、反転、解除。選択境界の点滅表示（マーチングアンツ）
5. **フィルタとの結合** — P4の全フィルタが選択範囲内のみに適用されるようにする

受け入れ条件:

- 自動選択＋フェザー＋フィルタ適用の一連操作がUndo/Redoで正しく往復する
- 選択マスクがプロジェクト保存・自動保存に含まれ、復旧後も維持される
- 4096×4096画像での自動選択がUIをブロックしない

リスク: Fabric.jsのオブジェクトモデルとピクセルマスクの整合が最難関。実装前に設計ADRを`docs/adr`へ追加し、レンダリング方式（クリップパス方式 / 合成後マスク方式）を比較検証してから本実装に入る。

### P7: 背景除去とスクリプトコンソール

作業項目:

1. **背景除去（ローカルAI）** — ONNX Runtime Web + 軽量セグメンテーションモデル（u2netp級、数MB）で人物・主要被写体の背景を透過する。実行はWorker、WebGPU利用可能ならWebGPU EP、fallbackはWASM。結果はレイヤーマスクまたは新規レイヤーとして展開し、P6の選択ツールで手直しできるようにする
2. **モデル配布** — モデルはアプリ本体と分離した遅延ダウンロードとし、初回に取得サイズを明示して同意を得る。取得後はOPFSへキャッシュしオフラインで再利用する。これが既定で外部取得を行う唯一の機能になるため、設定画面とREADMEに明記する
3. **スクリプトコンソール** — P2のコマンド層を薄くラップした`editor.*` API（例: `editor.resize()`、`editor.applyFilter()`、`editor.forEachLayer()`）を公開し、JSスニペットを実行できるパネルを追加する。実行はサンドボックス化Worker内で行い、DOM・ネットワークへのアクセスを遮断する。スクリプトの保存・マクロとしての登録に対応する

受け入れ条件:

- 代表画像で背景除去が実用品質・実用時間（WASMで10秒以内目安）で完了し、結果を手動修正できる
- 機内モード（モデルキャッシュ済み）でも背景除去が動作する
- コンソールのスクリプトから`fetch`・`document`へアクセスできない（unitテストで担保）
- スクリプト実行が1つのUndo単位になる

リスク: モデル品質が期待に届かない可能性があるため、実装前に候補モデル2〜3種の品質・サイズ・速度をスパイクで比較し、結果を`docs/adr`に記録してから本実装を判断する。

## 3. 横断事項

### 3.1 品質・テスト

- 各フェーズで unit（Vitest）+ E2E（Playwright）+ axeアクセシビリティ監査を追加し、`npm run check`を維持する
- フィルタ・生成系はスクリーンショット比較（Playwrightのピクセル比較、許容差付き）を導入する
- `.pwx.json` schema更新のたびに、旧バージョン読み込みのゴールデンファイルテストを追加する

### 3.2 パフォーマンス予算

- メインスレッドのロングタスク: 単発操作で200ms以内を目標、超えるものはWorkerへ
- 初期バンドル: フィルタシェーダ・テンプレート・ONNXランタイムはすべて遅延ロードし、初期ロードサイズを現状から+10%以内に抑える

### 3.3 ドキュメント

- 各フェーズ完了時に`docs/IMPLEMENTATION_STATUS.md`を更新する
- 設計判断（コマンド層、選択マスク方式、モデル選定）は`docs/adr`へADRとして追加する
- READMEの機能一覧・ショートカット表を随時更新する

### 3.4 スコープ外（本計画では扱わない）

- サーバー連携、共同編集、クラウド保存
- 生成AIによる画像生成（テキストからの画像生成）。ロゴ生成はテンプレート合成方式とする
- PSD / XCF入出力
- モバイル最適化UI

## 4. 進め方

1フェーズ = 1ブランチ（例: `agent/p1-logo-foundation`）とし、PRごとに`npm run check`と既存E2E全通過を条件とする。P1とP2は依存がないため並行着手も可能だが、`App.tsx`の変更が競合しやすいため、直列（P1 → P2）を推奨する。

各フェーズの着手時に、本計画の該当セクションを具体的なタスクリストへ分解してから実装に入る。

## 5. 完了記録（2026-07-31）

P1〜P7の製品経路を実装し、Studio UI、renderer-neutralな純粋ロジック、Fabric.js adapter、Worker、保存形式、境界testへ接続した。詳細な実装状況と意図的な制約は[実装状況](./docs/IMPLEMENTATION_STATUS.md)を正本とする。

| フェーズ | 完了した成果                                                                                                                                                            |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1       | 安全なSVG入出力、装飾テキスト、整列・分布、ルーラーからドラッグするガイド・スナップ、schema version 2とv1 migration                                                     |
| P2       | 検証可能な操作コマンド、semantic target、記録・パラメータ化・import/export、成功時Undo 1回・失敗時rollbackするmacro                                                     |
| P3       | decode→command→encode Worker、Transferable、進捗・cancel、50件の受入確認済みbatch UI、フォルダ出力/ZIP fallback、7種のicon preset                                       |
| P4       | 14種のfilter registryと実画像preview付きparameter UI、version付きpreset、custom WebGL・CPU fallback、下位合成へ追従して再編集・表示切替・ラスタライズできる調整レイヤー |
| P5       | 主要色抽出、4種の配色調和、20件の検証済みtemplate、seed付き候補生成、lock/再生成、編集可能レイヤーへの展開                                                              |
| P6       | lossless 8bit mask、pointerなげなわ、自動選択、4合成mode、フェザー・形態演算、マーチングアンツ、ブラシ/消しゴムclip、pixel-delete、選択フィルターWorker                 |
| P7       | pinned U2NetPの同意付き遅延取得、SHA-256/OPFS、ONNX Runtime WebのWebGPU→WASM、背景除去Worker、安全なDSL、saved scripts、`runScript` macro                               |

P7のスクリプトは、当初案の「生JavaScriptをWorkerへ渡して遮断する」方式を採用しなかった。Workerにも`fetch`等の能力が残り、`eval`禁止CSPと両立しないため、限定grammarをcommandへ変換する`eval`なしDSLを採用した。保存・macro登録後もsourceを再parseし、macroと同じatomic transactionで実行する。この判断は[ADR-007](./docs/adr/0007-background-model-and-script-security.md)に記録した。

P4のフィルタプリセットはP2レシピと同じversioning・runtime validation・端末内repositoryの境界を再利用する一方、`.pwxmacro.json`へ直接格納しない。P2の基本`applyFilter` commandと、曲線点列・色・seedを含むP4 registry operationではparameter形状とbatch能力が異なり、`runScript`文字列への変換は型とdiagnosticを失うためである。version付きcompanion schemaを採用した判断と将来の統合条件は[ADR-008](./docs/adr/0008-automation-and-worker-pipeline.md)へ記録した。

背景モデルは候補比較の結果、一般被写体にも使える軽量なU2NetPを採用した。descriptorは4,574,861 bytes、Apache-2.0、SHA-256 `309c8469258dda742793dce0ebea8e6dd393174f89934733ecc8b14c76f4ddd8`、immutable revision URLへ固定した。実モデルのWASM受け入れscriptでは640×480 synthetic画像を約981ms、NASAの実写真512×512を約840msで処理し、10秒以内と前景・背景maskの閾値を満たした。実モデルをOPFS相当へ保存した後のoffline cache hitではnetwork callbackが0回であることも確認した。モデルbytesとONNX Runtime assetは初期entryとService Worker app shellへ含めない。

### 統合・検証

当初はphaseごとのbranch / PRを想定したが、今回はP1〜P7を最後まで一括で完成させる依頼に合わせ、`codex/feature-expansion`の1ブランチへ統合した。project schema、`App.tsx`、command、Worker protocol、選択maskがphaseをまたいで相互依存し、途中の不整合状態を個別に公開しないためである。最終的な変更は1件のPRでまとめてreviewする。

次の自動検証を追加した。

- schema v1→v2 golden migration、SVG安全境界、command/macro atomicity、Worker protocol・cancel・Transferable、ZIP/icon寸法
- 14 filterのCPU kernel・custom WebGL shader・実画像preview・preset・下位合成へ追従する調整レイヤー、4096×4096 levels CPU経路の2秒budget test
- palette/配色/template、選択mask codec・合成・flood fill・clip・pixel-delete・選択filter Worker
- model consent、固定descriptor、download size/hash、破損cache、offline cache hit、ONNX WebGPU→WASM fallback、DSL拒否・保存・`runScript`再検証
- production build時の初期entry +10% budgetと、ONNX Runtime assetがService Worker precacheへ入らないことの検査
- 実U2NetP fileに対するbytes、SHA-256、OPFS offline再利用、WASM推論時間、synthetic画像とNASA実写真のmask品質を検証する独立した受け入れscript
- Pages本番subpathで、50-file long-task、ZIP/icon寸法、filter macro round trip、magic-wand→feather→filter→Undo/Redo、主要panelのaxe、生成結果とフィルター結果のgolden screenshot

Chromium上の主要Studio操作はPlaywright対象へ追加した。一方、実機WebGPUの性能、Safari / Firefox実機、VoiceOver / NVDA、複数実画像corpusによるhair・半透明境界のmatting品質は、広い互換性・継続品質検証として残る。これらを「全ブラウザー・全被写体検証完了」とは扱わない。
